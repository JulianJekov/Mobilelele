spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 12345
    url: jdbc:mysql://localhost:3306/mobilelele?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
  thymeleaf: # Thymeleaf
      cache: false
      mode: HTML
      encoding: UTF-8
      prefix: file:src/main/resources/templates/
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        format_sql: true
  sql:
    init:
      mode: never

  mvc:
    hidden method:
      filter:
        enabled: true
  web:
    resources:
      static-locations: file:src/main/resources/static/
      cache:
        period: 0

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
#    org.springframework: debug

open.exchange.rates:
  schema: https
  host: openexchangerates.org
  path: /api/latest.json
  app_id: ${APP_ID:test}
  symbols: EUR,BGN
  enabled: false

https://openexchangerates:
  org/api/latest:
    json?app_id=d9e9c24df6a442fb9883c4d99478402e&symbols=EUR%2CBGN&prettyprint=false&show_alternative=false:

